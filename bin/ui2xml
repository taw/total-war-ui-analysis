#!/usr/bin/env ruby

require_relative "../lib/ui_file"
require_relative "../lib/xml_builder"

$stdout.sync = true

unless ARGV.size == 2
  STDERR.puts "Usage: #{$0} input.ui output.xml"
  exit 1
end

XMLBuilder.new(ARGV[1]) do |output|
  ui_file = UiFile.new(ARGV[0], output)
  ui_file.convert!
end
